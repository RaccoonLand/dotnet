# 1. Overview

`RaccoonLand.Templates.CapabilityCentric` is a `dotnet new` project template that scaffolds a capability-centric RaccoonLand application.

## What it generates

| Area | Content |
|------|---------|
| Application project | `{ProjectName}` |
| Host project | `{ProjectName}.Hosting.API` |
| Capability | `People` with `FirstName` and `LastName` |
| Endpoints | `CreatePerson`, `GetPersonById`, `SearchPeople` |
| Persistence | SQL Server command/query DbContexts ready for you to create the initial EF Core migration |
| Infrastructure | OpenAPI, Scalar, Serilog, observability, validation, request caching, exception handling |

## Design goals

1. Keep the generated solution small and easy to extend.
2. Preserve RaccoonLand capability-centric folder layout.
3. Allow project and DbContext names to be customized at creation time.
4. Ship as a standalone solution with its own `global.json`, `Directory.Packages.props`, and `nuget.config`.

## Template identity

| Property | Value |
|----------|-------|
| Package ID | `RaccoonLand.Templates.CapabilityCentric` |
| Template short name | `raccoonland-capability` |
| Source name token | `CapabilityCentricTemplate` |
| Command DbContext token | `TemplateCommandDbContext` |
| Query DbContext token | `TemplateQueryDbContext` |

## Repository layout

The template source in the monorepo is split into packaging metadata and shippable content:

```text
CapabilityCentricTemplate/
├── RaccoonLand.Templates.CapabilityCentric.csproj   # NuGet packaging only
├── docs/                                            # maintainer documentation
└── content/                                         # everything shipped by dotnet new
    ├── .template.config/template.json
    ├── CapabilityCentricTemplate.slnx
    ├── global.json
    ├── nuget.config
    ├── Directory.Build.props
    ├── Directory.Packages.props
    └── src/
        ├── CapabilityCentricTemplate/
        └── CapabilityCentricTemplate.Hosting.API/
```

Only `content/` is packed into the template NuGet. The generated project layout matches `content/` (without `.template.config`).

## Related docs

- [2. Installation](2.Installation.md)
- [3. Template Parameters](3.TemplateParameters.md)
- [4. Generated Solution Structure](4.GeneratedSolutionStructure.md)
- [5. Packaging and Publishing](5.PackagingAndPublishing.md)
- [6. Create Initial Migration](6.CreateInitialMigration.md)
